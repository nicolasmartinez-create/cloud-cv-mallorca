version: 1
frontend:
  phases:
    build:
      commands:
        - sed -i "s|__API_ENDPOINT__|$API_ENDPOINT|g" frontend/js/counter.js
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
